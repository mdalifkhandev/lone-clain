import React from 'react';

const LoanStatus = () => {
    return (
        <div>
            <h2 className="font-semibold text-lg">Loan Status</h2>
            <p>Your loan application is currently being processed.</p>
        </div>
    );
};

export default LoanStatus;